/**
 * @openzeppelin/hardhat-tron-upgrades
 *
 * Upgrades plugin for Hardhat on TRON. Exposes `hre.upgrades`-style helpers
 * (deployProxy, upgradeProxy, deployBeacon, validateUpgrade, ...) backed by
 * `@openzeppelin/upgrades-core` for upgrade-safety validation and
 * `@openzeppelin/hardhat-tron` for TronWeb-based deployment.
 *
 * Not yet implemented.
 */

export {};
